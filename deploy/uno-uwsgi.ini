[uwsgi]
base = /var/www/html/u-no

app = uno
module = %(app)

home = %(base)/.venv
pythonpath = %(base)

socket = %(base)/deploy/%n.sock

chmod-socket = 666

callable = app

logto = %(base)/deploy/log/%n.log
